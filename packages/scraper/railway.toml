[build]
builder = "nixpacks"

[deploy]
startCommand = "pnpm --filter @whisky-hunter/scraper exec tsx src/worker-entry.ts"
healthcheckPath = ""
restartPolicyType = "always"

[environments.production.variables]
NODE_ENV = "production"
