{
  "name": "@whisky-hunter/shared",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "echo 'no shared tests yet' && exit 0",
    "lint": "echo 'no lint yet' && exit 0",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "zod": "4.4.2",
    "ioredis": "5.10.1"
  }
}
