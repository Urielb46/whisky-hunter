---
phase: 01-data-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - turbo.json
  - tsconfig.base.json
  - .gitignore
  - .env.example
  - apps/api/package.json
  - apps/api/tsconfig.json
  - apps/api/src/index.ts
  - packages/database/package.json
  - packages/database/tsconfig.json
  - packages/database/vitest.config.ts
  - packages/database/src/index.ts
  - packages/database/src/__tests__/.gitkeep
  - packages/scraper/package.json
  - packages/scraper/tsconfig.json
  - packages/scraper/vitest.config.ts
  - packages/scraper/src/index.ts
  - packages/scraper/src/adapters/__tests__/.gitkeep
  - packages/scraper/src/resolver/__tests__/.gitkeep
  - packages/scraper/src/queue/__tests__/.gitkeep
  - packages/shared/package.json
  - packages/shared/tsconfig.json
  - packages/shared/src/index.ts
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - DATA-05

must_haves:
  truths:
    - "pnpm install succeeds at repo root and installs all workspace deps"
    - "pnpm turbo test runs Vitest in @whisky-hunter/database, @whisky-hunter/scraper, @whisky-hunter/shared"
    - "TypeScript strict mode compiles all packages with zero errors"
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "Workspace package list (apps/*, packages/*)"
      contains: "packages:"
    - path: "turbo.json"
      provides: "Turbo pipeline for build, typecheck, test"
      contains: "\"test\":"
    - path: "packages/scraper/vitest.config.ts"
      provides: "Vitest config for scraper package"
      contains: "defineConfig"
    - path: "packages/database/vitest.config.ts"
      provides: "Vitest config for database package"
      contains: "defineConfig"
    - path: ".env.example"
      provides: "Environment variable template (no secrets)"
      contains: "DATABASE_URL="
  key_links:
    - from: "turbo.json"
      to: "package.json scripts"
      via: "pnpm turbo run <task>"
      pattern: "turbo run"
---

<objective>
Bootstrap the WhiskyHunter monorepo (pnpm + Turbo) with TypeScript strict mode, Vitest test infrastructure, and four workspace packages: `apps/api`, `packages/database`, `packages/scraper`, `packages/shared`. This Wave 0 plan creates the test scaffolding that downstream plans require so every later task has an `<automated>` verify command that runs.

Purpose: Without monorepo + test infra, no other plan can run `pnpm --filter ... test`. This plan unblocks all subsequent waves.
Output: Workspace directories, Vitest configs, .env.example, baseline package.json files for each workspace.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-data-foundation/01-RESEARCH.md
@.planning/phases/01-data-foundation/01-VALIDATION.md

<interfaces>
<!-- Versions locked by RESEARCH.md "Standard Stack" table — DO NOT change -->

Runtime + tools:
- Node.js 22 LTS
- pnpm 10.33.2
- TypeScript 5.x (strict mode mandatory)
- turbo 2.9.8
- vitest + @vitest/coverage-v8

Workspace package names (use exactly these):
- @whisky-hunter/api          (apps/api)
- @whisky-hunter/database     (packages/database)
- @whisky-hunter/scraper      (packages/scraper)
- @whisky-hunter/shared       (packages/shared)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Initialize pnpm workspace, Turbo, root tsconfig, .gitignore, .env.example</name>
  <files>
    package.json,
    pnpm-workspace.yaml,
    turbo.json,
    tsconfig.base.json,
    .gitignore,
    .env.example
  </files>
  <read_first>
    .planning/phases/01-data-foundation/01-RESEARCH.md (Standard Stack section, Recommended Monorepo Structure section),
    .planning/phases/01-data-foundation/01-VALIDATION.md (Test Infrastructure table)
  </read_first>
  <action>
Create the monorepo root files with these EXACT contents/values:

1. `package.json` (repo root):
   - `"name": "whisky-hunter"`
   - `"private": true`
   - `"packageManager": "pnpm@10.33.2"`
   - `"engines": { "node": ">=22.0.0", "pnpm": ">=10.33.0" }`
   - scripts:
     - `"build": "turbo run build"`
     - `"typecheck": "turbo run typecheck"`
     - `"test": "turbo run test"`
     - `"test:integration": "turbo run test:integration"`
     - `"lint": "turbo run lint"`
     - `"clean": "turbo run clean && rm -rf node_modules"`
   - devDependencies:
     - `"turbo": "2.9.8"`
     - `"typescript": "5.7.3"`
     - `"@types/node": "22.10.2"`
     - `"tsx": "4.21.0"`

2. `pnpm-workspace.yaml`:
```yaml
packages:
  - "apps/*"
  - "packages/*"
```

3. `turbo.json`:
```json
{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "typecheck": {
      "dependsOn": ["^build"]
    },
    "test": {
      "dependsOn": ["^build"]
    },
    "test:integration": {
      "dependsOn": ["^build"],
      "cache": false
    },
    "lint": {},
    "clean": {
      "cache": false
    }
  }
}
```

4. `tsconfig.base.json`:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "lib": ["ES2022"],
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true
  }
}
```

5. `.gitignore` MUST include:
```
node_modules/
dist/
.turbo/
.env
.env.local
.env.*.local
*.log
coverage/
.vitest-cache/
playwright-report/
test-results/
```

6. `.env.example` (template only, NO secrets) MUST include exactly these lines:
```
# PostgreSQL (Neon for dev, Railway for prod)
DATABASE_URL=postgresql://user:password@localhost:5432/whisky_hunter

# Redis (BullMQ + cache; Railway Redis for prod)
REDIS_URL=redis://localhost:6379

# Scraper proxy (Bright Data residential — leave blank in dev)
PROXY_URL=

# Node env
NODE_ENV=development

# Scraper feature flags
SCRAPER_HEADLESS=true
SCRAPER_DEFAULT_CRON=0 2 * * *
```

Mitigates threat T-cred-leak: `.env` is gitignored; `.env.example` contains template only — no real credentials.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p=JSON.parse(fs.readFileSync('package.json','utf8'));if(p.packageManager!=='pnpm@10.33.2')process.exit(1);if(!p.devDependencies.turbo)process.exit(1);if(!p.scripts.test)process.exit(1);console.log('root pkg ok')" &amp;&amp; node -e "const fs=require('fs');const t=JSON.parse(fs.readFileSync('turbo.json','utf8'));if(!t.tasks.test)process.exit(1);if(!t.tasks['test:integration'])process.exit(1);console.log('turbo ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` exists at repo root and contains the literal string `"packageManager": "pnpm@10.33.2"`
    - `package.json` scripts contains `"test": "turbo run test"` and `"test:integration": "turbo run test:integration"`
    - `pnpm-workspace.yaml` exists and contains the literal lines `- "apps/*"` and `- "packages/*"`
    - `turbo.json` exists and `tasks.test` and `tasks["test:integration"]` are defined
    - `tsconfig.base.json` exists with `"strict": true` and `"noUncheckedIndexedAccess": true`
    - `.gitignore` contains `.env` and `node_modules/`
    - `.env.example` contains `DATABASE_URL=` and `REDIS_URL=` and does NOT contain any real password (no `sk-`, no real hostnames beyond `localhost`)
    - File `.env` is NOT created by this task
  </acceptance_criteria>
  <done>Root monorepo files committed; `pnpm install` (run later in task 3) will resolve workspaces.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Scaffold four workspace packages (api, database, scraper, shared) with Vitest configs and test directory placeholders</name>
  <files>
    apps/api/package.json,
    apps/api/tsconfig.json,
    apps/api/src/index.ts,
    packages/database/package.json,
    packages/database/tsconfig.json,
    packages/database/vitest.config.ts,
    packages/database/src/index.ts,
    packages/database/src/__tests__/.gitkeep,
    packages/scraper/package.json,
    packages/scraper/tsconfig.json,
    packages/scraper/vitest.config.ts,
    packages/scraper/src/index.ts,
    packages/scraper/src/adapters/__tests__/.gitkeep,
    packages/scraper/src/resolver/__tests__/.gitkeep,
    packages/scraper/src/queue/__tests__/.gitkeep,
    packages/shared/package.json,
    packages/shared/tsconfig.json,
    packages/shared/src/index.ts
  </files>
  <read_first>
    package.json (root, just created in Task 1),
    tsconfig.base.json (root, just created in Task 1),
    .planning/phases/01-data-foundation/01-RESEARCH.md (Recommended Monorepo Structure section, Standard Stack table)
  </read_first>
  <action>
Create each workspace package with the EXACT versions and names below. Each package has the same tsconfig.json shape (extends root base).

Common per-package `tsconfig.json` template:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": ["src/**/*"],
  "exclude": ["dist", "node_modules", "**/*.test.ts", "**/__tests__/**"]
}
```

1. `packages/shared/package.json`:
```json
{
  "name": "@whisky-hunter/shared",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "echo 'no shared tests yet' && exit 0",
    "lint": "echo 'no lint yet' && exit 0",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "zod": "4.4.2"
  }
}
```
   `packages/shared/src/index.ts`: `export {};`
   `packages/shared/tsconfig.json`: use template above.

2. `packages/database/package.json`:
```json
{
  "name": "@whisky-hunter/database",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "tsx src/migrate.ts",
    "db:seed": "tsx src/seed/index.ts",
    "lint": "echo 'no lint yet' && exit 0",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "drizzle-orm": "0.45.2",
    "postgres": "3.4.9",
    "zod": "4.4.2",
    "@whisky-hunter/shared": "workspace:*"
  },
  "devDependencies": {
    "drizzle-kit": "0.31.10",
    "vitest": "2.1.8",
    "@vitest/coverage-v8": "2.1.8",
    "tsx": "4.21.0",
    "typescript": "5.7.3"
  }
}
```
   `packages/database/src/index.ts`: `export {};` (placeholder; PLAN-02 fills in schema exports)
   `packages/database/vitest.config.ts`:
```ts
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    globals: true,
    environment: 'node',
    include: ['src/**/__tests__/**/*.test.ts'],
    coverage: {
      provider: 'v8',
      reporter: ['text', 'html'],
    },
  },
});
```
   Create empty file `packages/database/src/__tests__/.gitkeep`.

3. `packages/scraper/package.json`:
```json
{
  "name": "@whisky-hunter/scraper",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:integration": "vitest run --config vitest.integration.config.ts",
    "scrape:test": "tsx src/cli/scrape-once.ts",
    "lint": "echo 'no lint yet' && exit 0",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "bullmq": "5.76.5",
    "ioredis": "5.10.1",
    "playwright": "1.59.1",
    "playwright-extra": "4.3.6",
    "puppeteer-extra-plugin-stealth": "2.11.2",
    "fastest-levenshtein": "1.0.16",
    "robots-parser": "3.0.1",
    "zod": "4.4.2",
    "drizzle-orm": "0.45.2",
    "postgres": "3.4.9",
    "@whisky-hunter/database": "workspace:*",
    "@whisky-hunter/shared": "workspace:*"
  },
  "devDependencies": {
    "vitest": "2.1.8",
    "@vitest/coverage-v8": "2.1.8",
    "tsx": "4.21.0",
    "typescript": "5.7.3"
  }
}
```
   `packages/scraper/src/index.ts`: `export {};`
   `packages/scraper/vitest.config.ts`:
```ts
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    globals: true,
    environment: 'node',
    include: ['src/**/__tests__/**/*.test.ts'],
    exclude: ['**/node_modules/**', '**/integration/**'],
    coverage: {
      provider: 'v8',
      reporter: ['text', 'html'],
    },
  },
});
```
   Create three empty placeholder files:
     - `packages/scraper/src/adapters/__tests__/.gitkeep`
     - `packages/scraper/src/resolver/__tests__/.gitkeep`
     - `packages/scraper/src/queue/__tests__/.gitkeep`

4. `apps/api/package.json`:
```json
{
  "name": "@whisky-hunter/api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "dev": "tsx watch src/index.ts",
    "test": "echo 'api tests in later phases' && exit 0",
    "lint": "echo 'no lint yet' && exit 0",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "hono": "4.6.14",
    "@hono/node-server": "1.13.7",
    "@whisky-hunter/database": "workspace:*",
    "@whisky-hunter/shared": "workspace:*"
  },
  "devDependencies": {
    "tsx": "4.21.0",
    "typescript": "5.7.3"
  }
}
```
   `apps/api/src/index.ts` (Phase 1 health endpoint stub only):
```ts
import { Hono } from 'hono';
import { serve } from '@hono/node-server';

const app = new Hono();

app.get('/health', (c) => c.json({ status: 'ok', service: 'whisky-hunter-api' }));

const port = Number(process.env.PORT ?? 3000);
serve({ fetch: app.fetch, port });
console.log(`API listening on :${port}`);
```
   `apps/api/tsconfig.json`: use template above.

Each package's `tsconfig.json` MUST extend `../../tsconfig.base.json` (verifiable with grep).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const pkgs=['apps/api','packages/database','packages/scraper','packages/shared'];for(const p of pkgs){const j=JSON.parse(fs.readFileSync(p+'/package.json','utf8'));if(!j.name.startsWith('@whisky-hunter/'))process.exit(1);const tc=fs.readFileSync(p+'/tsconfig.json','utf8');if(!tc.includes('tsconfig.base.json'))process.exit(1);}if(!fs.existsSync('packages/database/vitest.config.ts'))process.exit(1);if(!fs.existsSync('packages/scraper/vitest.config.ts'))process.exit(1);console.log('packages ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Files exist: `apps/api/package.json`, `packages/database/package.json`, `packages/scraper/package.json`, `packages/shared/package.json`
    - Each package.json `name` field starts with literal `@whisky-hunter/`
    - `packages/database/package.json` lists `drizzle-orm` at version `0.45.2` and `drizzle-kit` at `0.31.10`
    - `packages/scraper/package.json` lists `bullmq` at `5.76.5`, `playwright` at `1.59.1`, `fastest-levenshtein` at `1.0.16`
    - `packages/database/vitest.config.ts` contains `defineConfig` and `include: ['src/**/__tests__/**/*.test.ts']`
    - `packages/scraper/vitest.config.ts` contains `defineConfig` and excludes integration tests
    - Each package `tsconfig.json` contains the literal string `tsconfig.base.json`
    - Test directory placeholders exist: `packages/scraper/src/adapters/__tests__/.gitkeep`, `packages/scraper/src/resolver/__tests__/.gitkeep`, `packages/scraper/src/queue/__tests__/.gitkeep`, `packages/database/src/__tests__/.gitkeep`
    - `apps/api/src/index.ts` contains `app.get('/health'`
  </acceptance_criteria>
  <done>All four packages scaffolded; tsconfig and Vitest configs in place.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Run pnpm install, install Playwright Chromium, verify pnpm turbo test passes (smoke)</name>
  <files>
    pnpm-lock.yaml
  </files>
  <read_first>
    package.json (root),
    pnpm-workspace.yaml,
    packages/database/package.json,
    packages/scraper/package.json
  </read_first>
  <action>
Run these commands in order from repo root:

1. `pnpm install`
   - Resolves all workspace packages and their dependencies.
   - Generates `pnpm-lock.yaml`.

2. `pnpm --filter @whisky-hunter/scraper exec playwright install chromium`
   - Downloads Chromium binary required by Playwright (Pitfall: scraper crashes at runtime without this).
   - This is REQUIRED in dev env; production Dockerfile will use `mcr.microsoft.com/playwright` base image (configured in PLAN-06).

3. Add a smoke test to confirm Vitest works. Create `packages/scraper/src/__tests__/smoke.test.ts`:
```ts
import { describe, it, expect } from 'vitest';

describe('smoke', () => {
  it('vitest is wired correctly', () => {
    expect(1 + 1).toBe(2);
  });
});
```

   And `packages/database/src/__tests__/smoke.test.ts`:
```ts
import { describe, it, expect } from 'vitest';

describe('smoke', () => {
  it('vitest is wired correctly', () => {
    expect(true).toBe(true);
  });
});
```

4. Run `pnpm turbo test` — MUST pass with all packages green.
5. Run `pnpm turbo typecheck` — MUST pass with zero TS errors.

If `pnpm install` fails on Windows due to Playwright postinstall, document in commit message and proceed — Chromium download can be re-run manually. Do NOT skip the install step.

Mitigates threat T-cred-leak: `pnpm-lock.yaml` is committed (lockfile is not a secret); `.env` remains gitignored.
  </action>
  <verify>
    <automated>pnpm turbo test --filter=@whisky-hunter/database --filter=@whisky-hunter/scraper</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm-lock.yaml` exists at repo root and contains `@whisky-hunter/database`, `@whisky-hunter/scraper`, `@whisky-hunter/shared`, `@whisky-hunter/api` (literal strings)
    - `node_modules/` exists at repo root (workspace hoisting)
    - `packages/scraper/src/__tests__/smoke.test.ts` exists and imports from `vitest`
    - `packages/database/src/__tests__/smoke.test.ts` exists
    - `pnpm turbo test --filter=@whisky-hunter/database --filter=@whisky-hunter/scraper` exits 0 (both smoke tests green)
    - `pnpm turbo typecheck` exits 0
    - Playwright Chromium binary downloaded — `node_modules/.cache/ms-playwright` or equivalent path exists (best-effort; if Windows postinstall fails, capture log in commit message)
  </acceptance_criteria>
  <done>Workspaces installed, Vitest verified working, Chromium installed, typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem -> git | `.env` files containing secrets must never enter version control |
| developer machine -> npm registry | Dependency tarballs are untrusted by default; pinned versions reduce supply-chain attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Information Disclosure | `.env` accidentally committed | mitigate | `.gitignore` includes `.env`, `.env.local`, `.env.*.local`; `.env.example` is the only env file in version control and contains placeholder values only (verifiable: no `sk-`, no real hostnames) |
| T-01-01-02 | Tampering | Supply-chain attack via floating dep ranges | mitigate | All package.json versions are exact (no `^` or `~` prefixes); `pnpm-lock.yaml` is committed; `packageManager` field pins pnpm version |
| T-01-01-03 | Denial of Service | TypeScript strict mode disabled hides bugs | mitigate | `tsconfig.base.json` sets `"strict": true` and `"noUncheckedIndexedAccess": true`; all packages extend the base |
</threat_model>

<verification>
- `pnpm install` completes without errors
- `pnpm turbo test` runs Vitest in database and scraper packages and exits 0
- `pnpm turbo typecheck` exits 0 across all four workspaces
- `git status` shows `.env` is untracked (not in any commit)
- All file paths listed in `files_modified` exist
</verification>

<success_criteria>
- Monorepo bootstraps cleanly on a fresh clone with `pnpm install`
- Vitest configs exist for both `@whisky-hunter/database` and `@whisky-hunter/scraper`
- TypeScript strict mode active across all packages
- `.env.example` is committed; `.env` is gitignored and uncommitted
- Smoke tests prove Vitest pipeline works end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation/01-01-SUMMARY.md`.
</output>
