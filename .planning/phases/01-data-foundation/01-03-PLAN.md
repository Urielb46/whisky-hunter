---
phase: 01-data-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-02
files_modified:
  - packages/database/src/migrations/0000_initial.sql
  - packages/database/src/migrations/9999_partition_create_helper.sql
  - packages/database/src/migrate.ts
  - packages/database/src/partitions.ts
  - packages/database/src/seed/index.ts
  - packages/database/src/seed/retailers.ts
  - packages/database/src/seed/products-bootstrap.ts
  - packages/database/src/seed/data/retailers.json
  - packages/database/src/seed/data/canonical-products.json
  - packages/database/src/__tests__/append-only-guard.test.ts
  - packages/database/src/__tests__/staleness-query.test.ts
  - packages/database/README.md
autonomous: true
requirements:
  - DATA-01
  - DATA-03
  - DATA-04
  - DATA-05

must_haves:
  truths:
    - "Database migration runs successfully and creates all 6 tables + materialized view"
    - "price_snapshots is partitioned by scraped_at; current month + next month partitions exist"
    - "REVOKE UPDATE, DELETE on price_snapshots from the application role enforces append-only at SQL level"
    - "Seed script populates retailers table with all 10 target retailers (DATA-01 prerequisite)"
    - "Canonical product seed creates >= 50 baseline products from LWIN/LCBO bootstrap data"
    - "Staleness query (scraped_at > now() - interval '48 hours') returns only fresh snapshots"
  artifacts:
    - path: "packages/database/src/migrations/0000_initial.sql"
      provides: "Drizzle-generated migration with manual PARTITION BY RANGE injection"
      contains: "PARTITION BY RANGE (scraped_at)"
    - path: "packages/database/src/migrate.ts"
      provides: "Migration runner script"
      exports: ["main"]
    - path: "packages/database/src/seed/data/retailers.json"
      provides: "Seed data for 10+ target retailers"
      min_lines: 50
    - path: "packages/database/src/seed/data/canonical-products.json"
      provides: "Bootstrap canonical product registry"
      min_lines: 50
    - path: "packages/database/src/__tests__/staleness-query.test.ts"
      provides: "DATA-05 staleness query verification"
      min_lines: 20
    - path: "packages/database/README.md"
      provides: "Documents do-not-introspect rule"
      contains: "drizzle-kit introspect"
  key_links:
    - from: "packages/database/src/migrate.ts"
      to: "src/migrations/*.sql"
      via: "drizzle-orm/postgres-js/migrator"
      pattern: "migrate\\("
    - from: "packages/database/src/seed/index.ts"
      to: "retailers and products tables"
      via: "db.insert"
      pattern: "db\\.insert"

user_setup:
  - service: neon-or-railway-postgres
    why: "Phase 1 needs a real PostgreSQL 16 instance to run drizzle-kit generate + migrate against"
    env_vars:
      - name: DATABASE_URL
        source: "Neon dashboard -> Connection string (use pooled URL for app, direct URL for migrations)"
    dashboard_config:
      - task: "Create Postgres 16 database (Neon free tier or Railway)"
        location: "console.neon.tech or railway.app"
  - service: redis
    why: "BullMQ requires Redis from PLAN-04 onwards; install during this plan to verify connection"
    env_vars:
      - name: REDIS_URL
        source: "Railway Redis service Connect tab, or local docker run"
    dashboard_config:
      - task: "Provision Redis 7 instance (Railway service or local docker run -p 6379:6379 redis:7)"
        location: "railway.app -> New -> Database -> Redis"
---

<objective>
Generate the initial Drizzle SQL migration, MANUALLY inject `PARTITION BY RANGE (scraped_at)` for price_snapshots, run the migration against the live PostgreSQL instance, seed the retailers and bootstrap canonical product registry, and add a monthly partition auto-creation helper. This is a **[BLOCKING]** plan: every later plan depends on a working schema and a populated retailers/products foundation. Adds SQL-level append-only enforcement via REVOKE.

Purpose: DATA-01 (10+ retailers configured) requires retailers seeded. DATA-02 (canonical product registry) requires the bootstrap registry to exist. DATA-03 (append-only) requires both Drizzle-side (no update helpers, PLAN-02) AND SQL-side (REVOKE) enforcement. DATA-05 (48h staleness) requires the query to be tested.
Output: Live migrated DB with all 6 tables + materialized view + 2 monthly partitions + 10+ retailers + 50+ canonical products.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-data-foundation/01-RESEARCH.md
@.planning/phases/01-data-foundation/01-VALIDATION.md
@.planning/phases/01-data-foundation/01-01-SUMMARY.md
@.planning/phases/01-data-foundation/01-02-SUMMARY.md

<interfaces>
<!-- Drizzle migration workflow (locked by RESEARCH.md and schema_push_requirement) -->

ALLOWED:
- pnpm --filter @whisky-hunter/database run db:generate    # generates SQL into src/migrations
- pnpm --filter @whisky-hunter/database run db:migrate     # applies SQL via custom migrator script
- pnpm --filter @whisky-hunter/database run db:seed        # populates retailers + bootstrap products

PROHIBITED:
- drizzle-kit push        — silently drops columns, RESEARCH.md flags as production-unsafe
- drizzle-kit introspect  — overwrites schema after manual partition SQL is added; corrupts schema (Pitfall 1)

Target retailers (10+ minimum, MUST cover at least UK/US/EU/CA per RESEARCH.md):
  whisky-exchange, master-of-malt, the-whisky-barrel, abbey-whisky (UK)
  total-wine, specs, kl-wines (US)
  la-maison-du-whisky (FR), whisky-de (DE)
  lcbo (CA)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: [BLOCKING] Generate migration, manually inject partition DDL + materialized view + REVOKE, run migrate, write README do-not-introspect note</name>
  <files>
    packages/database/src/migrations/0000_initial.sql,
    packages/database/src/migrations/9999_partition_create_helper.sql,
    packages/database/src/migrate.ts,
    packages/database/src/partitions.ts,
    packages/database/README.md
  </files>
  <read_first>
    packages/database/drizzle.config.ts (created in PLAN-02),
    packages/database/src/schema/price-snapshots.ts (created in PLAN-02 — defines composite PK),
    .planning/phases/01-data-foundation/01-RESEARCH.md (Pattern 3 Drizzle Schema with partitioning workaround, Pitfall 1, Pitfall 4, Code Examples / Materialized View)
  </read_first>
  <action>
This task is **[BLOCKING]**: no scraper plan can run until this completes successfully against a live PostgreSQL.

Required env: `DATABASE_URL` must point to a PostgreSQL 16 instance (Neon dev DB acceptable). Use the user_setup directives at the top of this plan if not yet provisioned.

Step A — Generate the base migration via Drizzle Kit:
```
pnpm --filter @whisky-hunter/database run db:generate
```
This creates `packages/database/src/migrations/0000_<hash>_initial.sql`. Rename the file to `0000_initial.sql` for determinism.

Step B — MANUALLY edit `0000_initial.sql` to APPEND (after the Drizzle-generated CREATE TABLE statements) the following SQL block. Drizzle will have generated `CREATE TABLE price_snapshots (...)` with a composite PK on (id, scraped_at) because the schema declares it. The price_snapshots CREATE TABLE must be modified so that the partition clause is added inline (PostgreSQL does not allow `ALTER TABLE ... PARTITION BY` after creation).

Required modifications/appends to `0000_initial.sql`:

1. Find the `CREATE TABLE "price_snapshots"` statement Drizzle produced. REPLACE that statement with the following (exact text):
```sql
CREATE TABLE "price_snapshots" (
  "id"                    bigserial NOT NULL,
  "canonical_product_id"  uuid NOT NULL,
  "source_mapping_id"     uuid NOT NULL,
  "currency"              char(3) NOT NULL,
  "price_local"           numeric(10,2) NOT NULL,
  "price_usd"             numeric(10,2),
  "in_stock"              boolean NOT NULL DEFAULT true,
  "scraped_at"            timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT "price_snapshots_pkey" PRIMARY KEY ("id", "scraped_at"),
  CONSTRAINT "price_snapshots_canonical_product_id_fkey"
    FOREIGN KEY ("canonical_product_id") REFERENCES "products"("id"),
  CONSTRAINT "price_snapshots_source_mapping_id_fkey"
    FOREIGN KEY ("source_mapping_id") REFERENCES "source_mappings"("id")
) PARTITION BY RANGE ("scraped_at");
```

2. Append (at the END of the file) the partition + materialized view + append-only enforcement DDL:
```sql
-- ============================================================
-- Manually appended (Drizzle does not generate any of the below)
-- DO NOT run drizzle-kit introspect after this point.
-- See packages/database/README.md and RESEARCH.md Pitfall 1.
-- ============================================================

-- Indexes on the parent table (cascade to partitions in PG 16)
CREATE INDEX "price_snapshots_product_scraped_idx"
  ON "price_snapshots" ("canonical_product_id", "scraped_at");
CREATE INDEX "price_snapshots_source_scraped_idx"
  ON "price_snapshots" ("source_mapping_id", "scraped_at");

-- Initial partitions: current month + next month (Pitfall 4)
-- The partition runtime helper (partitions.ts) creates future months on the 25th.
CREATE TABLE "price_snapshots_2026_05"
  PARTITION OF "price_snapshots"
  FOR VALUES FROM ('2026-05-01') TO ('2026-06-01');

CREATE TABLE "price_snapshots_2026_06"
  PARTITION OF "price_snapshots"
  FOR VALUES FROM ('2026-06-01') TO ('2026-07-01');

-- DATA-03: Append-only enforcement at the SQL level.
-- Application role (whose name MUST match the role used by the postgres client in db.ts)
-- is granted INSERT and SELECT only. UPDATE and DELETE are revoked.
-- For local dev where DATABASE_URL uses the superuser, this REVOKE applies but the
-- superuser BYPASSES it — production must use a non-superuser role.
DO $$
DECLARE
  app_role text := current_user;
BEGIN
  EXECUTE format('REVOKE UPDATE, DELETE ON TABLE price_snapshots FROM %I', app_role);
  EXECUTE format('GRANT  SELECT, INSERT ON TABLE price_snapshots TO %I',  app_role);
END$$;

-- DATA-05 / Phase 2 prerequisite: materialized view of best price per product (last 48h)
CREATE MATERIALIZED VIEW "current_best_prices" AS
SELECT
  p.id              AS product_id,
  MIN(ps.price_usd) AS min_price_usd,
  MAX(ps.scraped_at) AS latest_scraped_at,
  COUNT(DISTINCT ps.source_mapping_id) FILTER (WHERE ps.in_stock) AS source_count
FROM products p
JOIN source_mappings sm ON sm.canonical_product_id = p.id
JOIN price_snapshots ps ON ps.source_mapping_id    = sm.id
WHERE ps.scraped_at > now() - interval '48 hours'
GROUP BY p.id;

CREATE UNIQUE INDEX "current_best_prices_product_idx"
  ON "current_best_prices" (product_id);
```

Step C — Create `packages/database/src/migrate.ts`:
```ts
import postgres from 'postgres';
import { drizzle } from 'drizzle-orm/postgres-js';
import { migrate } from 'drizzle-orm/postgres-js/migrator';
import path from 'node:path';
import { fileURLToPath } from 'node:url';

const __filename = fileURLToPath(import.meta.url);
const __dirname = path.dirname(__filename);

async function main() {
  const url = process.env.DATABASE_URL;
  if (!url) throw new Error('DATABASE_URL is required to run migrations');
  const client = postgres(url, { max: 1 });
  const db = drizzle(client);
  await migrate(db, { migrationsFolder: path.join(__dirname, 'migrations') });
  await client.end();
  console.log('migrations applied');
}

main().catch((err) => {
  console.error(err);
  process.exit(1);
});
```

Step D — Create `packages/database/src/partitions.ts` (helper used by the BullMQ monthly partition job in PLAN-04, Pitfall 4):
```ts
import { sql } from 'drizzle-orm';
import { db } from './db';

/**
 * Idempotently create the partition for a given (year, month).
 * Mitigates Pitfall 4: scraper writes on the 1st of a new month fail without a partition.
 */
export async function ensurePartitionForMonth(year: number, month: number): Promise<void> {
  const start = `${year}-${String(month).padStart(2, '0')}-01`;
  const nextMonth = month === 12 ? 1 : month + 1;
  const nextYear = month === 12 ? year + 1 : year;
  const end = `${nextYear}-${String(nextMonth).padStart(2, '0')}-01`;
  const partitionName = `price_snapshots_${year}_${String(month).padStart(2, '0')}`;
  await db.execute(sql.raw(
    `CREATE TABLE IF NOT EXISTS "${partitionName}" ` +
    `PARTITION OF "price_snapshots" ` +
    `FOR VALUES FROM ('${start}') TO ('${end}');`
  ));
}

export async function ensureCurrentAndNextMonthPartitions(now: Date = new Date()): Promise<void> {
  const y = now.getUTCFullYear();
  const m = now.getUTCMonth() + 1;
  await ensurePartitionForMonth(y, m);
  const ny = m === 12 ? y + 1 : y;
  const nm = m === 12 ? 1 : m + 1;
  await ensurePartitionForMonth(ny, nm);
}
```

Step E — Create `packages/database/src/migrations/9999_partition_create_helper.sql` (no-op marker so devs see the partition convention):
```sql
-- Marker: future monthly partitions are created at runtime by ensureCurrentAndNextMonthPartitions().
-- See packages/database/src/partitions.ts and the BullMQ monthly job in @whisky-hunter/scraper.
SELECT 1;
```

Step F — Run the migration:
```
pnpm --filter @whisky-hunter/database run db:migrate
```
The migrate step MUST exit 0. If it fails, capture the error in the commit message and stop.

Step G — Create `packages/database/README.md` with EXACTLY this content:
```markdown
# @whisky-hunter/database

PostgreSQL 16 + Drizzle ORM 0.45.2.

## Migration Workflow (Locked)

```
pnpm --filter @whisky-hunter/database run db:generate
# (manual: edit src/migrations/000N_*.sql to add any partition/materialized view DDL)
pnpm --filter @whisky-hunter/database run db:migrate
```

## DO NOT RUN

- `drizzle-kit push`         — drops unrecognized columns silently. Production-unsafe.
- `drizzle-kit introspect`   — sees partitioned `price_snapshots` as multiple top-level tables and overwrites schema. See RESEARCH.md Pitfall 1.

## price_snapshots is APPEND-ONLY

- TypeScript: no `db.update(priceSnapshots)` or `db.delete(priceSnapshots)` in any code path.
- SQL: `REVOKE UPDATE, DELETE ON TABLE price_snapshots FROM <app_role>` is executed by `0000_initial.sql`.
- Schema changes to this table go in NEW migration files; never re-generate.

## Monthly partitions

- Created at runtime by `ensureCurrentAndNextMonthPartitions()` (src/partitions.ts).
- A BullMQ job (in @whisky-hunter/scraper) runs this on the 25th of every month.
```

Mitigates threats T-mutation (REVOKE), T-cred-leak (DATABASE_URL never logged in migrate.ts).
  </action>
  <verify>
    <automated>pnpm --filter @whisky-hunter/database run db:migrate &amp;&amp; node -e "const fs=require('fs');const sql=fs.readFileSync('packages/database/src/migrations/0000_initial.sql','utf8');if(!sql.includes('PARTITION BY RANGE'))process.exit(1);if(!sql.includes('current_best_prices'))process.exit(1);if(!sql.includes('REVOKE UPDATE, DELETE'))process.exit(1);if(!sql.includes('price_snapshots_2026_05'))process.exit(1);console.log('migration sql ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `packages/database/src/migrations/0000_initial.sql` exists and contains literal string `PARTITION BY RANGE ("scraped_at")` (note quotes)
    - Same file contains `CREATE MATERIALIZED VIEW "current_best_prices"`
    - Same file contains `REVOKE UPDATE, DELETE ON TABLE price_snapshots`
    - Same file contains `CREATE TABLE "price_snapshots_2026_05"` and `CREATE TABLE "price_snapshots_2026_06"`
    - File `packages/database/src/migrate.ts` exists and imports from `drizzle-orm/postgres-js/migrator`
    - File `packages/database/src/partitions.ts` exports `ensurePartitionForMonth` and `ensureCurrentAndNextMonthPartitions`
    - `pnpm --filter @whisky-hunter/database run db:migrate` exits 0 against the configured DATABASE_URL
    - `packages/database/README.md` exists and contains the literal strings `drizzle-kit introspect`, `drizzle-kit push`, and `APPEND-ONLY`
    - After migrate, querying `SELECT count(*) FROM information_schema.tables WHERE table_schema='public'` returns >= 6 tables (manual check; commit log should note this)
  </acceptance_criteria>
  <done>Schema migrated to PostgreSQL with partitioning, materialized view, and SQL-level append-only enforcement.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Seed retailers (10+) and bootstrap canonical product registry (50+ products from LWIN/LCBO baseline)</name>
  <files>
    packages/database/src/seed/index.ts,
    packages/database/src/seed/retailers.ts,
    packages/database/src/seed/products-bootstrap.ts,
    packages/database/src/seed/data/retailers.json,
    packages/database/src/seed/data/canonical-products.json
  </files>
  <read_first>
    packages/database/src/schema/retailers.ts (column names),
    packages/database/src/schema/products.ts (column names),
    .planning/phases/01-data-foundation/01-RESEARCH.md (Target Retailers section, Phase Requirements section DATA-02)
  </read_first>
  <action>
DATA-02 mandates the canonical product registry exist BEFORE the first scraper runs. This task seeds:
- 10+ retailers (DATA-01 prerequisite)
- 50+ canonical baseline products from publicly known SKUs (LWIN-style bootstrap; future scraper output will reconcile against this)

1. `packages/database/src/seed/data/retailers.json` — write the following 10 retailer rows verbatim (per RESEARCH.md Target Retailers table):
```json
[
  { "id": "whisky-exchange",      "name": "The Whisky Exchange",       "baseUrl": "https://www.thewhiskyexchange.com",   "country": "GB", "currency": "GBP", "scraperType": "playwright", "catalogUrl": "https://www.thewhiskyexchange.com/c/40/single-malt-scotch-whisky", "cronExpression": "0 2 * * *",  "affiliateProgram": true,  "tosStatus": "ambiguous", "active": true },
  { "id": "master-of-malt",       "name": "Master of Malt",            "baseUrl": "https://www.masterofmalt.com",         "country": "GB", "currency": "GBP", "scraperType": "playwright", "catalogUrl": "https://www.masterofmalt.com/whiskies/",                            "cronExpression": "30 2 * * *", "affiliateProgram": true,  "tosStatus": "ambiguous", "active": true },
  { "id": "whisky-barrel",        "name": "The Whisky Barrel",         "baseUrl": "https://www.thewhiskybarrel.com",      "country": "GB", "currency": "GBP", "scraperType": "playwright", "catalogUrl": "https://www.thewhiskybarrel.com/whisky",                            "cronExpression": "0 3 * * *",  "affiliateProgram": false, "tosStatus": "ambiguous", "active": true },
  { "id": "abbey-whisky",         "name": "Abbey Whisky",              "baseUrl": "https://www.abbeywhisky.com",          "country": "GB", "currency": "GBP", "scraperType": "playwright", "catalogUrl": "https://www.abbeywhisky.com/collections/all",                       "cronExpression": "30 3 * * *", "affiliateProgram": false, "tosStatus": "ambiguous", "active": true },
  { "id": "total-wine",           "name": "Total Wine & More",         "baseUrl": "https://www.totalwine.com",            "country": "US", "currency": "USD", "scraperType": "playwright", "catalogUrl": "https://www.totalwine.com/spirits/scotch/c/000884",                "cronExpression": "0 4 * * *",  "affiliateProgram": false, "tosStatus": "ambiguous", "active": true },
  { "id": "specs",                "name": "Spec's Wine, Spirits & Finer Foods", "baseUrl": "https://www.specsonline.com", "country": "US", "currency": "USD", "scraperType": "playwright", "catalogUrl": "https://www.specsonline.com/spirits/scotch.html",                  "cronExpression": "30 4 * * *", "affiliateProgram": false, "tosStatus": "ambiguous", "active": true },
  { "id": "kl-wines",             "name": "K&L Wine Merchants",        "baseUrl": "https://www.klwines.com",              "country": "US", "currency": "USD", "scraperType": "playwright", "catalogUrl": "https://www.klwines.com/Products?filters=sv2_207%3D33",            "cronExpression": "0 5 * * *",  "affiliateProgram": false, "tosStatus": "ambiguous", "active": true },
  { "id": "la-maison-du-whisky",  "name": "La Maison du Whisky",       "baseUrl": "https://www.whisky.fr",                "country": "FR", "currency": "EUR", "scraperType": "playwright", "catalogUrl": "https://www.whisky.fr/whisky.html",                                 "cronExpression": "30 5 * * *", "affiliateProgram": false, "tosStatus": "ambiguous", "active": true },
  { "id": "whisky-de",            "name": "Whisky.de",                 "baseUrl": "https://www.whisky.de",                "country": "DE", "currency": "EUR", "scraperType": "playwright", "catalogUrl": "https://www.whisky.de/shop/",                                       "cronExpression": "0 6 * * *",  "affiliateProgram": false, "tosStatus": "ambiguous", "active": true },
  { "id": "lcbo",                 "name": "LCBO",                       "baseUrl": "https://www.lcbo.com",                  "country": "CA", "currency": "CAD", "scraperType": "playwright", "catalogUrl": "https://www.lcbo.com/en/products?categories=spirits/whisky",       "cronExpression": "30 6 * * *", "affiliateProgram": false, "tosStatus": "ambiguous", "active": true }
]
```

2. `packages/database/src/seed/data/canonical-products.json` — write at least 50 canonical products covering popular SKUs across distilleries. Use this format:
```json
[
  { "name": "Glenfarclas 15 Year Old", "distillery": "Glenfarclas", "ageYears": 15, "volumeMl": 700, "category": "scotch_single_malt", "region": "Speyside",  "caskType": "Sherry", "abv": 46.0, "lwinCode": null },
  { "name": "Glenfarclas 25 Year Old", "distillery": "Glenfarclas", "ageYears": 25, "volumeMl": 700, "category": "scotch_single_malt", "region": "Speyside",  "caskType": "Sherry", "abv": 43.0, "lwinCode": null },
  { "name": "Ardbeg 10 Year Old",      "distillery": "Ardbeg",      "ageYears": 10, "volumeMl": 700, "category": "scotch_single_malt", "region": "Islay",     "caskType": "Bourbon","abv": 46.0, "lwinCode": null }
]
```

The file MUST contain >= 50 entries covering at least these distilleries (each as one or more expressions): Glenfarclas, Ardbeg, Lagavulin, Laphroaig, Glenfiddich, Macallan, Highland Park, Talisker, Bowmore, Dalmore, Springbank, GlenDronach, Balvenie, Glenmorangie, Aberlour, Bunnahabhain, Caol Ila, Glenlivet, Mortlach, Bruichladdich, plus US: Buffalo Trace, Maker's Mark, Knob Creek; Irish: Redbreast 12, Green Spot, Jameson 18; Japanese: Yamazaki 12, Hibiki Harmony, Nikka From The Barrel.

Acceptance only requires count >= 50; the assistant should select reasonable canonical SKUs covering Scotch single malt, bourbon, Irish, Japanese categories.

3. `packages/database/src/seed/retailers.ts`:
```ts
import { db } from '../db';
import { retailers } from '../schema/retailers';
import data from './data/retailers.json' with { type: 'json' };

export async function seedRetailers(): Promise<number> {
  let inserted = 0;
  for (const r of data) {
    const result = await db
      .insert(retailers)
      .values(r as typeof retailers.$inferInsert)
      .onConflictDoNothing({ target: retailers.id })
      .returning({ id: retailers.id });
    inserted += result.length;
  }
  console.log(`retailers: inserted ${inserted}, total in file: ${data.length}`);
  return inserted;
}
```

4. `packages/database/src/seed/products-bootstrap.ts`:
```ts
import { db } from '../db';
import { products } from '../schema/products';
import { sql } from 'drizzle-orm';
import data from './data/canonical-products.json' with { type: 'json' };

export async function seedCanonicalProducts(): Promise<number> {
  // Skip if products already exist — bootstrap is one-time.
  const [{ count }] = await db.execute<{ count: number }>(
    sql`SELECT count(*)::int AS count FROM products`,
  );
  if (count > 0) {
    console.log(`canonical products: ${count} already exist, skipping bootstrap seed`);
    return 0;
  }
  const inserted = await db
    .insert(products)
    .values(data as Array<typeof products.$inferInsert>)
    .returning({ id: products.id });
  console.log(`canonical products: inserted ${inserted.length}`);
  return inserted.length;
}
```

5. `packages/database/src/seed/index.ts`:
```ts
import { seedRetailers } from './retailers';
import { seedCanonicalProducts } from './products-bootstrap';
import { ensureCurrentAndNextMonthPartitions } from '../partitions';
import { queryClient } from '../db';

async function main() {
  await ensureCurrentAndNextMonthPartitions();
  await seedRetailers();
  await seedCanonicalProducts();
  await queryClient.end();
}

main().catch((err) => {
  console.error(err);
  process.exit(1);
});
```

Run: `pnpm --filter @whisky-hunter/database run db:seed`. MUST exit 0.
  </action>
  <verify>
    <automated>node -e "const r=require('./packages/database/src/seed/data/retailers.json');if(r.length&lt;10)process.exit(1);const c=require('./packages/database/src/seed/data/canonical-products.json');if(c.length&lt;50)process.exit(1);console.log('seed data counts ok: retailers='+r.length+' products='+c.length)" &amp;&amp; pnpm --filter @whisky-hunter/database run db:seed